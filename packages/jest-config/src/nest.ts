// Jest config for NestJS apps and Nest-flavored packages.
// Identical to base today; kept as its own entry so Nest-specific tweaks
// (e.g. reflect-metadata setup for decorator-driven tests) land in one place.
// Consume via: export { default } from '@tickethub/jest-config/nest'
export { default } from './base';
